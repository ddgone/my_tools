# 火蜥蜴工具箱

这是一个围绕“本地工具执行、远程工具执行、工具分发导出”构建的桌面工具平台。它的核心不是某一种 UI 技术，而是让同一批工具以一致、低门槛、可扩展的方式被使用、连接和分发。

## Language

### 产品形态

**桌面宿主**:
用户日常打开和操作的主应用。它承载工具目录、参数输入、执行结果、任务管理、远程连接与导出入口。
_Avoid_: TUI, 主程序壳子, 桌面版前端

**工具平台**:
整个产品体系，包含 **桌面宿主**、**工具**、**远程执行** 与 **导出能力**。当讨论长期演进时，用这个词而不是仅说“界面”。
_Avoid_: UI, 壳, 客户端

**能力迁移**:
把旧项目中已经存在的可用能力迁入新平台，但不承诺保留原先的界面结构、交互步骤或快捷键形式。迁移的对象是能力本身，而不是旧 TUI 的具体表现。
_Avoid_: 原样复刻, 界面平移

**主路径**:
第一阶段必须被完整打通、用来验证平台是否成立的最小用户闭环。它优先验证工具平台的核心承诺，而不是追求功能面的完整覆盖。
_Avoid_: 全量 MVP, 大而全首版

### 运行对象

**工具**:
用户可以直接执行或导出的独立能力单元。一个 **工具** 可以由 Go 或 Python 实现，但对用户而言都属于同一种产品对象。
_Avoid_: 脚本, 命令, 插件

**工具规格**:
定义一个 **工具** 的统一描述对象。它必须能表达元数据、参数、运行方式、构建方式、远程执行方式与导出方式，而不再只是一个可直接调用的运行函数。
_Avoid_: 旧 Tool 接口, 运行回调

**工具清单**:
以文件形式存在、可被平台加载和复制的 **工具规格**。它负责描述工具的静态信息，而具体运行逻辑由对应适配器实现。
_Avoid_: 硬编码注册表, 仅代码配置

**Go 工具**:
以 Go 为主要实现语言的 **工具**。这类工具优先通过构建生成 **工具专属产物**，再用于本地执行、远程执行或导出。
_Avoid_: 原生命令, 内置命令

**HDFS工具**:
归在 `KD测试工具` 下的一类 **工具**，面向 HDFS/WebHDFS 数据拉取、检查或处理场景。它表达的是工具分类与使用场景，不等同于某一个具体脚本或一次性任务。
_Avoid_: HDFS脚本, 临时下载器

**Python 工具**:
以 Python 脚本为主要实现形态的 **工具**。这类工具的分发与远程执行以脚本源码为第一交付单位，而不是优先包装为独立二进制。
_Avoid_: 二进制 Python 工具, 打包版 Python 工具

**内置工具**:
由平台随安装包一起提供并由平台负责版本管理的 **工具**。用户可以使用它们，但不应直接在原位置修改其资产。
_Avoid_: 系统脚本, 默认脚本

**自定义工具**:
由用户从 **内置工具** 复制出来或自行创建、独立于平台升级周期管理的 **工具**。这类工具位于用户工作区，允许后续单独演进。
_Avoid_: 魔改内置工具, 临时副本

**模块边界**:
单仓库内各核心子系统之间清晰、长期稳定的职责分隔。它要求桌面宿主、工具规格、运行时、构建、目录管理与文档各自演进，而不是继续揉成一个整体。
_Avoid_: 大一统主程序, 杂糅结构

**单页工作台**:
IDE/Postman 风格的 UI 范式，整个应用在一个窗口内以"左侧常驻侧边栏 + 右侧动态工作区"的形式运行，不使用路由跳转。所有功能（工具浏览、配置、执行、日志）在同一视图中完成。
_Avoid_: 多页面跳转, 路由导航, 浏览器式交互

**工具页签**:
桌面上方横向排列的页签（Tab），每个 **打开的工具** 对应一个页签。用户可同时打开多个工具，通过点击页签在它们之间切换。页签按打开时间排序，最新操作优先。SSH 连接管理也复用同一套页签系统，工具页签与 SSH 页签共存于同一条 Tab 栏。
_Avoid_: 标签页

**侧边栏**:
始终显示在界面左侧的面板区域，由两层结构组成：最左侧的 **活动栏**（窄竖条图标按钮，用于视图切换）和右侧的可展开面板（工具分类树、搜索框、SSH 服务器列表等）。它是用户在 **单页工作台** 中浏览和选择工具、管理远程连接配置的入口。
_Avoid_: 左侧菜单, 导航栏

**活动栏**:
**侧边栏** 最左侧的窄竖条图标按钮栏，类似 VS Code 的 Activity Bar。包含 6 个功能图标（工具浏览器、SSH 连接管理、收藏夹、最近使用、任务中心、导出中心）外加底部系统设置入口。每个按钮对应一个侧边栏视图，点击切换侧边栏当前展示的面板内容。点击已激活的按钮收起侧边栏。任务中心和导出中心为占位项（功能尚未实现）。活动栏始终可见，是用户在侧边栏各视图间导航的主要方式。
_Avoid_: 底部切换按钮, 视图切换条

**侧边栏视图**:
活动栏打开的四种可切换面板：工具浏览器（NTree 多级分类树）、SSH 连接管理（NList + SSH 操作）、收藏夹（收藏的工具列表）、最近使用（最近的工具执行记录列表）。
_Avoid_: 展开面板

**执行终端**:
始终可见的底部日志输出面板。它是 **桌面宿主** 中流式日志的主要展示位置，支持清空、复制和导出日志。未执行时显示占位提示。
_Avoid_: 日志窗口, 输出面板

**结构化表单**:
桌面宿主默认使用的参数录入方式。用户通过字段、开关、路径选择器和下拉项来填写工具输入，而不是手写整段参数字符串。
_Avoid_: 纯命令行输入, 裸参数串

**命令行模式**（原"原始参数模式"，已重命名）:
面向高级用户的备用参数录入方式。它允许用户直接编辑最终传给工具的参数字符串，并与 **结构化表单** 共存。现在作为参数配置区内的一个 tab 存在。
_Avoid_: 唯一输入方式, 调试专用隐藏入口, 原始参数模式

**执行目标**:
一个 **工具** 实际运行的位置。当前明确分为 **本地执行目标** 与 **远程执行目标**。
_Avoid_: 环境, 机器, 终端

**本地执行目标**:
运行在用户当前桌面宿主所在机器上的 **执行目标**。
_Avoid_: 本机模式, 默认模式

**远程执行目标**:
通过 SSH 连接到远端主机后，由桌面宿主调度 **工具** 运行的 **执行目标**。
_Avoid_: SSH 模式, 服务器模式

**远程执行**:
由 **桌面宿主** 在本地统一发起和调度，但实际在 **远程执行目标** 上完成的工具运行方式。它与本地执行是同一 **工具** 的两种运行路径，而不是两套不同功能。
_Avoid_: 远程版工具, 服务器工具

## 连接管理

**SSH 连接**:
一组用于连接远程服务器的配置信息，包含名称、主机地址、端口、用户名、认证方式（密码或密钥）和备注。它由 **SSH 连接管理** 系统持久化存储在 **运行时目录** 的 `config/` 子目录下。
_Avoid_: 主机配置, 远程配置, 服务器条目

**SSH 连接管理**:
**桌面宿主** 内置的远程连接配置系统。用户在 **侧边栏** 的 SSH 服务器列表中浏览和管理所有连接，通过 **SSH 页签** 在右侧主工作区进行新建、编辑、测试和删除操作。每个连接提供三点菜单快捷操作（编辑、复制地址、测试连通性、删除）。
_Avoid_: 连接面板, 远程管理

**SSH 页签**:
与 **工具页签** 共存在同一条 Tab 栏中的页签，用于打开 SSH 连接的表单编辑界面。每个打开的 SSH 连接表单占据一个页签，和工具页签按打开时间统一排序，点击即可切换。
_Avoid_: 连接窗口, SSH 弹出框

**连接测试**:
在保存连接之前或之后，通过 **测试连接** 按钮主动向远程服务器发起 SSH 握手验证连通性。测试结果以颜色区分（成功绿色、失败红色）显示在表单底部。新建连接测试时不写入存储，编辑连接测试时从存储读取凭据。
_Avoid_: Ping, 连通性检查（可接受但非正式）

**密码显隐切换**:
**SSH 连接** 表单中密码输入框旁的 👁 按钮，允许用户在密文（password）和明文（text）之间切换，便于核对密码内容。
_Avoid_: 显示密码, 隐藏密码

**三点菜单**:
**侧边栏** SSH 列表项右侧的 `···` 按钮，点击弹出下拉菜单，提供编辑、复制地址、测试连通性、删除四个快捷操作，避免在狭窄列表中堆砌按钮。
_Avoid_: 更多操作, 右键菜单

## 窗口状态记忆

**窗口记忆**:
桌面宿主跨会话持久化窗口几何（尺寸、位置）、最大化状态与全屏状态并从配置恢复的机制。它确保用户关闭应用后再次打开时，窗口回到上一次的位置和状态，而不是每次从默认尺寸启动。
_Avoid_: 窗口缓存, 位置还原

**窗口化状态**:
窗口处于既非最大化、也非全屏时的几何（`width`, `height`, `x`, `y`）。它是 **窗口记忆** 的核心存储对象。app.json 中 `width/height/x/y` 的语义固定为"上一次窗口化时的几何"，**最大化状态或全屏状态下这些字段都不直接采样写盘**。
_Avoid_: 普通窗口大小, 还原尺寸

**最大化状态**:
一个布尔标记（`maximised`），与 **窗口化状态** 正交。它只标记窗口是否处于最大化，不存储最大化时的几何。恢复时会先恢复窗口化状态，再进入最大化。
_Avoid_: 伪全屏, 放大窗口

**全屏状态**:
一个布尔标记（`fullscreen`），表示窗口是否处于操作系统级全屏。它与 **最大化状态** 不同，尤其在 macOS 上是独立概念。恢复时会先恢复窗口化状态，再进入全屏。
_Avoid_: 最大化, fullscreen mode（当实际指最大化时）

**启动冷却期**:
前端 `onMounted` 后的一段静默时间（2 秒），期间不响应保存请求。这给 Go 侧 `OnDomReady` 足够时间完成窗口恢复并稳定，防止启动阶段的中间态几何被误写入 config。
_Avoid_: 启动延迟, 初始化等待

## 视觉与动画

**动画系统**:
基于 GSAP（GreenSock Animation Platform）构建的桌面应用动画框架。它负责管理页面切换过渡、侧边栏折叠展开、按钮交互反馈、文件树节点动效等所有前端交互动画，采用"常规克制 + 特殊炫酷"的混合风格策略。
_Avoid_: CSS 动画散落各处, 过渡仅为 show/hide

**启动画面 (Splash)**:
桌面宿主启动阶段可选的品牌过渡层，用于遮盖主工作台尚未就绪时的短暂空窗。它可以由 Go 原生窗口或前端遮罩承载，但只有在代码真实接通后，才允许在开发文档中把它描述为现行能力。
_Avoid_: 把未落地方案当作已上线能力, 启动白屏

**静态资源目录**:
安装镜像目录下 `assets/` 子目录，存放不需要嵌入 exe 的只读静态资源（如启动画面图片、字体文件、默认图标等）。这些资源由构建脚本从源码目录复制到 `build/image/host/assets/`，运行时由 Go 端通过 exe 相对路径读取。
_Avoid_: go:embed 嵌入所有资源, 资源散落在根目录

**动画优先级分批**:
动画实施按三批推进：第一批（Splash 过渡 + 页面切换动画），第二批（侧边栏展开折叠 + 按钮 hover/press 动效），第三批（文件树节点展开 + 终端滚动 + Modal 弹入弹出）。每批完成后可独立验证效果。
_Avoid_: 一次性全量动画, 不做分批规划

## 分发与产物

**导出能力**:
把一个 **工具** 从工具平台中提取为可独立分发产物的能力。它是桌面宿主之后的第二阶段核心能力。
_Avoid_: 打包, 发布, 复制脚本

**导出产物**:
由 **导出能力** 生成、可脱离 **桌面宿主** 单独分发和运行的结果。
_Avoid_: 安装包, 成品, 小程序

**工具专属产物**:
只包含单个 **工具** 所需能力的独立运行结果。它既是 **导出产物** 的目标形态，也是远程执行时优先采用的交付单位。
_Avoid_: 整个工具箱副本, 全量客户端

**Python 脚本产物**:
以完整脚本文件形式交付的 **工具专属产物**。它可放在桌面宿主安装目录中被直接调用，也可被直接导出或上传到远端执行。
_Avoid_: Python 二进制包, Python 黑盒产物

**运行时目录**:
桌面宿主实际读写缓存、脚本副本、日志、导出文件和连接配置的目录根。它服务于执行与分发，不等同于源码仓库目录。
_Avoid_: 项目目录, 源码目录, 临时目录

**开发态运行时目录**:
开发阶段放在仓库内 `build/runtime/` 下、用于快速迭代的 **运行时目录** 形态。它应尽量镜像安装后的目录结构，使开发态与发布态复用同一套运行逻辑。
_Avoid_: 构建输出目录, 源码工作区, 随机临时目录

**安装镜像目录**:
桌面宿主构建产物的最终落点，也是未来打包安装镜像的数据来源。它只包含只读资产（宿主程序、内置工具资产、工具链资产），不与可变数据混放。
_Avoid_: 构建产物目录, 导出目录, 运行目录

**工具链资产**:
可选放入 **安装镜像目录** 的构建工具链，如 Go 环境。它只服务于宿主端的单工具构建、远程执行和导出流程，绝不被包含进单工具导出产物。
_Avoid_: 系统 Go, 用户自装 Go

## Flagged Ambiguities

- “复制为自定义工具” 的复制粒度尚未明确。后续需要决定它在用户界面上是始终全量复制，还是允许高级用户按资产类型做裁剪。

## Example Dialogue

开发者: 这个能力先放进桌面宿主里跑，还是直接做成独立程序？
领域负责人: 先作为工具接入桌面宿主，等导出能力稳定后，再生成对应的导出产物。

开发者: 这个工具现在跑在哪里？
领域负责人: 先跑在本地执行目标，后面也必须支持远程执行目标。

开发者: 远程执行是不是单独的一套工具？
领域负责人: 不是，同一个工具应该尽量复用，只是执行目标不同。

开发者: 远程执行时要把整个工具箱都传过去吗？
领域负责人: 不要，优先传单个工具对应的工具专属产物，用完后清理远端，只在本地保留编译缓存。

开发者: Python 工具导出时要不要也想办法打成 exe？
领域负责人: 默认不要，先直接导出完整脚本，让安装目录、导出流程和远程执行都复用同一种 Python 脚本产物。

开发者: 安装目录里的 Python 脚本允许直接改吗？
领域负责人: 暂时不允许，内置工具属于平台管理资产；如果用户要改，先复制为自定义工具，再在用户工作区里单独演进。

开发者: 开发时说的“固定目录”是源码目录吗？
领域负责人: 不是，应该叫开发态运行时目录。它可以放在仓库里，但语义上仍然是运行时目录，要和源码区分开。

开发者: “安装包展开后一致的开发目录” 是不是就一个目录全装进去？
领域负责人: 不是，至少要区分安装镜像目录和运行时目录。安装镜像目录装只读资产，运行时目录承接缓存、日志、导出和配置。

开发者: 安装目录里带的 Go 环境是不是也应该跟着单工具导出产物一起走？
领域负责人: 不应该。Go 环境如果存在，应作为安装镜像目录中的工具链资产服务桌面宿主，而不是塞进每个单工具导出产物里。

开发者: 新项目是不是要把旧 TUI 的界面和快捷键一比一搬过去？
领域负责人: 不需要，目标是做能力迁移，不是原样复刻；所有交互都允许按桌面宿主的方式重做。

开发者: 第一阶段是不是要先把收藏、设置、搜索、任务中心全做完？
领域负责人: 不是，先围绕主路径打通闭环：选工具、配参数、本地执行、看结果、基础远程执行、清理远端；单工具导出放到下一阶段。

开发者: 桌面宿主里的参数输入先做成什么样？
领域负责人: 默认用结构化表单，同时保留原始参数模式，既体现桌面体验，也保留高级用户和复杂场景的兜底入口。
