# 用统一的工具规格替代旧 Tool 接口

现有只包含 `ID/Name/Category/Execute` 的旧 `Tool` 接口被彻底退役，新的核心模型改为统一的工具规格，用来描述工具元数据、参数模型、执行方式、构建方式、远程执行方式与导出方式。我们不再围绕旧接口做兼容扩展，因为新的桌面宿主、远程执行与导出能力都要求工具从“可被 TUI 调用的函数”升级为“可被平台编排的产物单元”，继续兼容旧模型只会把过时抽象带进新架构。
