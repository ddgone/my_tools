# 工具规格采用清单优先、适配器执行的模型

新的工具规格采用清单优先模型：每个工具先以文件化工具清单描述其元数据、参数、图标、说明、运行入口、远程执行策略与导出策略，再由对应的 Go 或 Python 适配器负责实际执行。我们不继续使用纯代码注册作为主模型，因为桌面宿主需要直接渲染工具目录、参数表单、复制为自定义工具、导出与远程执行，而这些能力都更依赖可读取、可复制、可校验的静态描述，而不是只能在编译后存在的代码结构。
