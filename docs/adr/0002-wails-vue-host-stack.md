# 使用 Wails + Vue 3 作为桌面宿主技术底座

桌面宿主采用 `Wails` 承载 Go 后端能力，并使用 `Vue 3` 构建本地化 Web 前端界面。我们没有选择继续深化 `tview`，也没有转向以 Rust 为主的 `Tauri` 或以 Node/Electron 为主的重桌面壳，因为当前项目最可复用、最有价值的资产在 Go 侧工具执行与编排能力，而桌面宿主需要优先释放 UI 表达力和复杂交互能力，同时尽量保留现有 Go 资产的迁移价值。
