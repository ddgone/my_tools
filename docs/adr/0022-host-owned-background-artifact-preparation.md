# 由桌面宿主接管后台产物准备与缓存复用

桌面宿主在导出与远程执行时统一在自身进程内后台准备工具专属产物：Windows 下隐藏构建子进程窗口，构建结果先进入运行时目录中的构建缓存，再按需要复制到导出目标或直接上传远端，并把准备阶段通过应用内进度反馈暴露给用户。我们不再依赖系统终端承载这段过程，因为用户感知的是导出能力和远程执行目标，而不是底层工具链窗口。当前产物中心采用“左侧专属侧边栏 + 右侧主页面”的工作区形态，右侧的“工具 × 平台矩阵”标题行承载工具筛选与搜索，左侧则承载产物工作台入口与任务历史快照入口。
