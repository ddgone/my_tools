# 安装镜像目录与运行时目录分离

我们决定把“安装包展开后的内容”与“应用运行后产生的可变数据”拆成两层：**安装镜像目录** 只包含桌面宿主、内置工具资产与可选 **工具链资产**，而 **运行时目录** 只承接缓存、脚本副本、日志、导出结果与配置。**工具链资产** 若存在，只服务桌面宿主的单工具构建、远程执行与导出，不进入单工具导出产物本身。这样开发态可以在仓库内同时模拟安装镜像和运行时布局，发布时又不会把缓存和日志错误打进安装包。
