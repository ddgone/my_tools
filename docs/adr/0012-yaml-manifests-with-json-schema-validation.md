# 工具清单使用 YAML，校验使用 JSON Schema

工具清单文件统一使用 `YAML` 表达，平台同时提供 `JSON Schema` 做静态校验与前后端共享约束。我们没有把 `JSON` 作为主格式，因为该平台后续明确要支持复制为自定义工具和人工编辑，而 `YAML` 更适合人读写；同时又没有放弃结构化校验，因为桌面宿主、构建器、远程执行和导出链路都需要在工具真正运行前就发现清单错误。
